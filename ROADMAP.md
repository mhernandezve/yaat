# YAAT Roadmap / Pending Tasks

See [DESIGN_DECISIONS.md](./DESIGN_DECISIONS.md) for architectural decisions and rationale.

---

## ✅ Recently Completed

- [x] Completely rewrite README for current version
- [x] Remove Windows support (Unix-only)
- [x] Add `yaat update` command with `--ask-unknown` flag
- [x] Implement `.yaatignore` support
- [x] Separate `config_dirs` and `home_files` structure
- [x] Add MIT `LICENSE` to repo
- [x] Verify `license = "MIT"` in `Cargo.toml`
- [x] Remove Windows from CI matrix
- [x] Rename `master` branch to `main`

---

## v0.1 — Public Release

### Immediate (no code)

- [x] ~~Add MIT `LICENSE` to repo~~
- [x] ~~Add `license = "MIT"` to `Cargo.toml`~~
- [x] ~~Remove Windows from CI matrix (`os: [ubuntu-latest, macos-latest]`)~~
- [x] ~~Rename `master` branch to `main`~~

_All immediate tasks completed. Moving to v0.1 CI/Tooling._

### CI / Tooling

- [ ] Add `cargo test --verbose` to CI workflow
- [x] Configure **lefthook** for local conventional commits
  - Validation via regex in `lefthook.yml`, no Node dependency
- [ ] Add **commitlint** job to CI (`wagoid/commitlint-github-action`)
- [ ] Configure **Release Please** for automated versioning
  - `release-type: rust`
  - Generates `CHANGELOG.md` and updates version in `Cargo.toml`
- [ ] Configure **cargo-dist** for release workflow
  - Targets: `x86_64-linux`, `aarch64-linux`, `x86_64-macos`, `aarch64-macos`
  - Generates installer script for bootstrap usage

### Commit Convention

Defined scopes: `cli`, `sync`, `manifest`, `encrypted`, `ci`, `deps`, `bootstrap`

Format: `type(scope): description`

```
feat(cli): add update command
fix(sync): resolve symlink conflict on macOS
chore(ci): add release-please workflow
```

### Distribution

- [ ] Pre-compiled binaries via GitHub Releases (managed by cargo-dist)
- [ ] Installer script generated by cargo-dist for bootstrap usage

### Bootstrap repo (`miguel/bootstrap`)

- [ ] Create `bootstrap` repo
- [ ] Single script that:
  1. Downloads and installs YAAT via installer script
  2. Runs `yaat init <repo-url>`
  3. Cleans up temporary files
- [ ] Single entry command:
```bash
curl -sL https://raw.githubusercontent.com/miguel/bootstrap/main/bootstrap.sh | bash -s work-laptop
```

### README

- [ ] Remove Windows references (supported platforms + symlinks section)
- [ ] Update manifest example — replace `include` with `config_dirs` and `home_files`
- [ ] Remove "Symlink or copy" from features — symlinks only
- [ ] Add `update` command in Quick Start and Project Structure (`commands/update.rs`)
- [ ] Remove references to `dotfiles-alt` (repo doesn't exist)
- [ ] Add conventional commits section in Contributing
- [ ] Update Installation with cargo-dist installer script
- [ ] Add mention to bootstrap repo

### Automated Tests

- [ ] Integration tests for `backup` command
- [ ] Integration tests for `sync` command
- [ ] Unit tests for yaat.yaml parsing
- [ ] Tests for path resolution functions
- [ ] Tests for symlink creation and validation

### Edge Case Handling

- [ ] Handle broken symlinks (symlink pointing to non-existent file)
- [ ] Handle conflicts (file exists and differs from repo)
- [ ] Handle permission issues (configs with 600 vs 644 permissions)
- [ ] Handle disk full scenarios
- [ ] Handle concurrent modifications

### Known Configs

- [ ] Structure as `KnownConfig` struct with categories to facilitate external contributions:
```rust
pub struct KnownConfig {
    pub name: &'static str,
    pub config_path: &'static str,
    pub category: Category,
}

pub enum Category {
    Shell, Editor, Terminal, WM, Bar, Notification, Other,
}
```

---

## v0.2 — Post-release

### Encrypted with age

- [ ] Add `age` crate as dependency (no external binary dependency)
- [ ] Add `encrypted` section to manifest:
```yaml
encrypted:
  enabled: false
  key: ~/.ssh/id_ed25519
  files:
    - .gitconfig
    - .ssh/config
```
- [ ] Create `encrypted/` folder in repo (`.age` files)
- [ ] Create `rendered/` folder (gitignored)
- [ ] Pipeline: `file.age` → decrypt → `rendered/` → symlink → `~/`
- [ ] Document main use cases: `.gitconfig` and `.ssh/config`
- [ ] Commands:
  - `yaat add .gitconfig --encrypt`
  - `yaat encrypt .gitconfig` (on already tracked file)

### Host-specific configs

- [ ] Activate commented section in manifest
- [ ] Allow exclusions by logical host:
```yaml
hosts:
  work-laptop:
    exclude:
      - hypr/desktop-specific.conf
```

### UX Improvements

- [ ] `yaat doctor` command — Verify repo health
  - Check for broken symlinks
  - Check for files in repo that don't exist in system
  - Check for configs in system not tracked in repo
  - Check for permission mismatches
  - Verify git repository integrity
  - Suggest fixes for found issues
- [ ] `yaat clean` command — Clean broken symlinks or uninstalled configs
  - Remove broken symlinks
  - Remove configs that have been uninstalled
  - Clean up old backup files (*.backup)
  - Option to dry-run before cleaning
- [ ] Better output
  - Add colors to output (green=OK, red=error, yellow=warning)
  - Add progress bars for long operations
  - Add summary at end (X added, Y ignored, Z errors, W skipped)
- [ ] Shell completions
  - Bash completion
  - Zsh completion
  - Fish completion

---

## v0.3 — TUI

- [ ] Implement as `yaat tui` subcommand (single binary)
- [ ] Dependencies: `ratatui` + `crossterm`
- [ ] Two main views:
  - **Interactive status** — file list with status (synced / not synced / modified), direct actions (`s` sync, `b` backup, `a` add)
  - **Manifest editor** — visual management of `config_dirs`, `home_files`, `encrypted`, exclusions without manual YAML editing
- [ ] Reuse existing logic from `config.rs` and `commands/` — only presentation layer

---

## 📝 Documentation (ongoing)

- [ ] Add man page (`man yaat`)
- [ ] Add `--help` examples for each command
- [ ] Video tutorial / GIF demos
- [ ] Migration guide from other tools (yadm, chezmoi, stow)

---

## 🟢 Nice-to-Have (Future Ideas)

- [ ] Templates — `yaat template save/load` for different setups
- [ ] Hooks — Scripts pre/post backup/sync
- [ ] Global configuration (`~/.config/yaat/config.toml`)
- [ ] Web interface for browsing configs
- [ ] Cloud sync integration (optional, manual git is preferred)
- [ ] Config validation (lint yaat.yaml)
- [ ] Import/export from other formats

---

Last updated: 2026-04-18
